from setuptools import setup, find_packages

setup(
    name="mbase",
    version="0.1.0",
    description="Удобная библиотека с базовыми инструментами",
    author="Ваше Имя",
    packages=find_packages(),
    install_requires=[],  # сюда добавьте сторонние зависимости, если есть
    python_requires=">=3.6",
)